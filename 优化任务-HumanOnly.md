
# ONGO 所有插件内置？

# Yapi 优化第0期
## ONGO 请求插件修改，支持 V3

## 接入 OA 的 oidc

# Yapi 优化改进 - 一期
## DONE 分组要能支持右键新页面打开

- 数据导入导出不再以插件存在，集成进主项目中
## 跳转登录后还要能跳回来 `?next=xxxx`
## 弹出框不要点击遮罩关闭
## 自定义项目文件夹的图片
## 接口右键菜单问题？？
## 顶部及底部标签栏 手工控制展开收起
+ 前端 localStorage 记录状态
+ 加载页面时读取状态，切换时更新记录

# yapi 优化建议 - 二期
## 核心数据结构改为 OpenApi 3 格式
## JSON schema 版本了解，如何升级？
+ 和 metersphere 一样，json scheme 能和 pure json 互相切换
+ 一键展开及收起
+ 请求体样例 响应体样例
## JSON schema 报错时，原样显示文本数据
## 屏幕宽度不够时，json schema 最右侧加号显示不出来
## 整体样式优化~~
## 批量导出及导入(组 接口 测例)
## 可任意定义 Mock 基础地址，如果冲突就弹出冲突提示即可
## 返回数据超大可选窗口展示
## 接口请求 互斥参数设置 联合参数设置（参数分组）
## 请求/响应数据界面 可选 上下显示(默认) 或者 左右列显示

# Yapi 优化改进 - 三期
## 访客模式，不注册登录也能浏览 API 接口
## 接口状态（自定义图标 / 自定义文字 / 自定义是否显示在列表前面） 接口列表前面显示接口状态
## typescript interface 返回格式定义（通过interface生成模拟数据）
## 测试接口可定义多套默认请求数据（单选按钮切换）（默认为第一个）
## query参数与body参数转换
## 接口参数处理方式
+ form数据
+ JSON数据
+ JSON+qs.stringify
## 接口字段输入界面可任意调整大小
+ 本地 LocalStorage 记录每个接口的界面大小


# ------ 需求池 ------
## 备注引用功能，一个接口的备注，会多个地方同步。
## 数据块引用，请求值、返回值 数据格式可引用自通用块
+ 禁止循环引用，必须独立为引用块，才能被其他数据引用
+ 引用块不支持引用其他引用块
+ 展示时，引用块内嵌到实际位置显示
+ 引用块就是普通字符串，无数据格式校验限制
## 全新的插件机制，插件不再零散碎片化。
## 返回数据及请求数据，直接复制类型文件。
+ ts 类型
+ rust 类型
+ golang 类型
+ php 类型
+ ... 自定义扩展接口
## 请求数据|返回数据 json 显示区域优化，可以全屏，可以拖动增加高度。
## openApi 3 格式的数据导出
+ redoc 集成
## 上下箭头切换显示接口
## 显示接口 创建时间 + 创建人 + 更新时间 + 更新人
## 使用 .d.ts 类型文件定义接口？
## 使用 grpc proto 类型文件定义接口？
## 请求测试 chrome app 支持，主要目的是取代 postman arc(advance rest client)
## 修改接口时，传递 原始值->修改后的值 ，在多人修改冲突时直接提醒？？

## 使用 yjs 做协同编辑？

## 支持 WebSocket ws wss 协议
包含 文档 测试 ...

## 测试模式下，点击复制出 curl 地址
+ Copy as cURL
+ Copy as PowerShell
+ Copy as fetch

## redux-promise 迁移到 redux-thunk

## 接口分组下的具体接口列表提供多种显示方式？？ 
至少提供 紧凑 适中 松散 三种列表状态可选吧

## 接口分组也提供一个显示界面，以可视化的形式表示该分组属于哪个部分
就像一个2D游戏一样，平面上各个区域标记为分组名称，点击进入该分组。。。

## 数据库改为 mysql 支持？？（可同时支持 mysql && mongo）

## 接口文件提供远程编辑链接 ？？

## 右侧栏及中间栏不全局滚动，只在自己的区域内滚动

## 测试-高级参数设置
+ 支持写代码，支持全局变量记忆
+ 支持运行历史保留，可选历史快照保留数量
+ 定义可持久化的全局变量 ??
+ 支持直接写js函数，返回值作为 mock 参数 ??

## 搜索接口
+ 好用的搜索界面，类似搜索引擎的显示

## 全局标签
+ 定义全局标签
+ 标签可以有 icon 及文字
+ 可使用全局标签及项目局部标签
+ 根据标签 筛选

## 每个接口单独的 编辑历史 显示
+ 放置在 预览 - 编辑 -... 顶栏同级别
+ 采用 git 文本行的形式进行对比 diff patch tools
+ 版本 Undo / Redo
+ 改动人是谁？

## 自定义状态 - 配置添加任意自定义勾选框
+ 前端标记完成 + 后端标记完成
+ TAG 自定义背景及文字颜色

## 接口字段输入界面可任意调整大小
+ 本地 LocalStorage 记录每个接口的界面大小

## Mock地址添加 代理模式 切换
+ 关闭代理模式时，返回模拟数据
+ 打开代理模式时，直接反向代理到设置的后端地址
+ 前端设置 (开发模式下)
请求地址都对应到 yapi Mock 地址上  
当后端接口没有数据时，Mock 地址返回 mock 值  
当后端某个接口已经完成，通过 Mock 地址转发请求到后端接口  
所以，开发模式时，不会去请求后端接口，只会请求 Mock 地址
由 yapi 控制对于请求是返回模拟数据还是转发其他后端，yapi 界面上提供切换按钮。

## postgresql 数据库支持
## mysql 数据库支持

## 关联 Git ，本身提供 Git 仓库，~~或者使用代码 Git 仓库~~？？
+ ~~返回数据提供远程编辑链接（通过IDE远程连接编辑）~~
+ 使用 git 维护测试脚本，~~或者这些脚本本身就在项目中~~
  + 每个项目都有一个 Yapi 生成的 Git 仓库，专门放置测试及模拟文件，该仓库内文件也由 Yapi 生成，与测试脚本一一对应
+ 使用 .yapi.jsonc 配置本仓库与 yapi 的关系
  + 配置注释与接口的关系，OpenApi规范兼容

## 接口数据采取 修改、确认 两段操作模式
+ 建议一个人修改，另一个人确认 *也可以自己修改自己确认，不限制确认人*

## 通用记忆逻辑？该页面有如下输入条目可记忆，点选要记忆的条目

## json schema 备注输入 界面改为 textarea
## 备注支持 MarkDown 格式
## json schema 类型 添加字符串枚举支持
## json 条目拖动调整顺序，甚至拖动调整层级

## 请求及响应定义界面分三栏，左边是 json schema，中间是 json 样例，右边是 type 类型

## 改动详情 界面优化
+ 以原始数据进行对比
+ 可切换以最终数据结构进行对比
  
## 右上角搜索优化

## 页面内文本搜索逻辑优化，当前 请求数据、返回数据 中滚动条隐藏的区域无法搜索

## 鼠标显示 其他人 的鼠标，给人一种协同编辑的效果

# ISSUE: https://github.com/YMFE/yapi/issues/2381
